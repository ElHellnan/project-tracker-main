{
  "name": "project-tracker-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:full": "concurrently \"npm run dev:server\" \"npm run dev\"",
    "build": "cd client && npm run build",
    "netlify-build": "mkdir -p netlify/data && cp server/dev.db netlify/data/prod.db && cd client && npm install && npm run build",
    "lint": "npm run lint --prefix client && npm run lint --prefix server",
    "typecheck": "npm run typecheck --prefix client && npm run typecheck --prefix server",
    "test": "npm run test --prefix client && npm run test --prefix server",
    "db:migrate": "npm run db:migrate --prefix server",
    "db:seed": "npm run db:seed --prefix server",
    "db:studio": "npm run db:studio --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.0.1",
    "husky": "^8.0.3",
    "lint-staged": "^13.2.0"
  },
  "workspaces": [
    "client",
    "server"
  ]
}